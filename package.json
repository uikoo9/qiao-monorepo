{
  "name": "root",
  "private": true,
  "scripts": {
    "init": "lerna bootstrap",
    "clean": "lerna clean -y",
    "ls": "lerna ls -l",
    "show": "lerna ls --graph"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
