'use strict';

// q
import { 
    success,
    info,
    warning,
    danger,
} from 'qiao-json';

export const jsonSuccess = success;
export const jsonInfo = info;
export const jsonWarning = warning;
export const jsonDanger = danger;